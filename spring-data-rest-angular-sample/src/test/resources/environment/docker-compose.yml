version: '2'
services:
  web:
    build: tomcat/.
    ports:
     - "8080"
    depends_on:
      - db
    links:
      - db:mysql
  db:
    build: mysql/.
    expose:
     - "3306"
    environment:
           MYSQL_ROOT_PASSWORD: my-secret-pw